{
  "name": "cjs-test",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "vitest run --globals"
  },
  "devDependencies": {
    "express-zod-api": "workspace:*",
    "zod": "catalog:dev"
  }
}
