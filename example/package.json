{
  "private": true,
  "name": "example",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build:docs && bun build:client",
    "build:docs": "bun run generate-documentation.ts",
    "build:client": "bun run generate-client.ts",
    "test": "vitest run --globals index.spec.ts",
    "validate": "vitest run --globals validate.spec.ts"
  },
  "dependencies": {
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "@types/swagger-ui-express": "^4.1.8"
  }
}
