{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "yarn do index.ts",
    "build": "yarn build:swagger && yarn build:client",
    "build:swagger": "yarn do generate-open-api-schema.ts",
    "build:client": "yarn do generate-client.ts && yarn prettier example.client.ts --write",
    "test": "jest"
  }
}
