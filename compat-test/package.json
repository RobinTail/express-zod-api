{
  "name": "compat-test",
  "type": "module",
  "private": true,
  "scripts": {
    "pretest": "echo \"import { DocumentationError } from 'express-zod-api';\" > sample.ts",
    "test": "oxlint --fix && vitest --run",
    "posttest": "rm sample.ts"
  },
  "devDependencies": {
    "@express-zod-api/migration": "workspace:*",
    "express": "npm:express@5.1.0",
    "express-zod-api": "workspace:*",
    "http-errors": "npm:http-errors@2.0.1",
    "typescript": "npm:typescript@6.0.3",
    "zod": "npm:zod@4.3.4"
  }
}
