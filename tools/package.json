{
  "name": "tools",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "do": "do.sh"
  },
  "scripts": {
    "build:tests": "yarn build:int && yarn build:esm",
    "build:int": "yarn do integration-test.ts",
    "build:esm": "yarn do esm-test.ts",
    "build:logo": "yarn do startup-logo.ts",
    "build:license": "yarn do license.ts"
  }
}
